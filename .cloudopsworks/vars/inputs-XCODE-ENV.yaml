# This file contains the environment variables for the Kubernetes deployment
environment: "dev|uat|prod|demo"
#runner_set: "macos-14"
#disable_deploy: true
xcode:
  configuration: Debug | Release | <...>

# The following are for cloud specific configurations
# These will be used to filter the secrets from the secret manager
# and created as secrets in the Kubernetes cluster, then inyected as environment variables
# For Azure:
#azure:
#  resource_group: RESOURCE_GROUP # Optional: set this if the build and deploy resource groups are the same
#  build_resource_group: RESOURCE_GROUP # Optional: set this if the build and deploy resource groups are different
#  deploy_resource_group: RESOURCE_GROUP # Optional: set this if the build and deploy resource groups are different
#  keyvayult_name: KEYVAULT_NAME
#  keyvault_secret_filter: KEYVAULT_SECRET_FILTER
#  pod_identity:
#    enabled: true
#    identity_name: IDENTITY_NAME
#
# For AWS
#aws:
#  region: AWS_REGION
#  sts_role_arn: "BUILD_AWS_STS_ROLE_ARN" # Optional: set this if the build and deploy roles are same
#  build_sts_role_arn: "BUILD_AWS_STS_ROLE_ARN" # Optional: set this if the build and deploy roles are different
#  deploy_sts_role_arn: "DEPLOY AWS_STS_ROLE_ARN" # Optional: set this if the build and deploy roles are different
#  secrets_path_filter: /secrets
#  interactive_sesions: true
#  device_farm_name: DEVICE_FARM_NAME
#  device_farm_pool: DEVICE_FARM_POOL_NAME # (optional) If there is a Farm Pool Defined if not, define specific device below.
#  device:                                 # (optional) Required if Farm Pool is not defined.
#    form_factor: "PHONE"
#    model: "Apple iPhone 14 Pro"
#    os_version: "17.3.1"
#    max_devices: 1
#
#  test_script_path: device_tests
#  test_script_type: APPIUM_JAVA_JUNIT_TEST_PACKAGE | APPIUM_JAVA_TESTNG_TEST_PACKAGE | APPIUM_PYTHON_TEST_PACKAGE | APPIUM_NODE_TEST_PACKAGE | APPIUM_RUBY_TEST_PACKAGE | APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE | APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE | APPIUM_WEB_PYTHON_TEST_PACKAGE | APPIUM_WEB_NODE_TEST_PACKAGE | APPIUM_WEB_RUBY_TEST_PACKAGE | CALABASH_TEST_PACKAGE | INSTRUMENTATION_TEST_PACKAGE | UIAUTOMATION_TEST_PACKAGE | UIAUTOMATOR_TEST_PACKAGE | XCTEST_TEST_PACKAGE | XCTEST_UI_TEST_PACKAGE | APPIUM_JAVA_JUNIT_TEST_SPEC | APPIUM_JAVA_TESTNG_TEST_SPEC | APPIUM_PYTHON_TEST_SPEC | APPIUM_NODE_TEST_SPEC | APPIUM_RUBY_TEST_SPEC | APPIUM_WEB_JAVA_JUNIT_TEST_SPEC | APPIUM_WEB_JAVA_TESTNG_TEST_SPEC | APPIUM_WEB_PYTHON_TEST_SPEC | APPIUM_WEB_NODE_TEST_SPEC | APPIUM_WEB_RUBY_TEST_SPEC | INSTRUMENTATION_TEST_SPEC | XCTEST_UI_TEST_SPEC
#  # Interactive Session Configuration - Use with caution could skyrocket costs
#  session_config:
#    form_factor: "PHONE"
#    model: "Apple iPhone 14 Pro"
#    os_version: "17.3.1"
#
#  pod_identity:
#    enabled: true
#    iam_role_name: ROLE_NAME
#
# For GCP
#gcp:
#  secrets_path_filter: /secrets
#  region: "GCP_REGION"
#  project_id: "GCP_PROJECT"
#  impersonate_sa: "GCP project Service Account e-mail" # Optional: when existing role is same for build and deploy
#  build_impresonate_sa: "GCP project Service Account e-mail" # Optional: when existing role is different for build and deploy
#  deploy_impersonate_sa: "GCP project Service Account e-mail" # Optional: when existing role is different for build and deploy
#  pod_identity:
#    enabled: true
#    service_account_name: SERVICE_ACCOUNT_NAME

